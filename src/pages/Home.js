import React from "react";
import Listitem from "../components/Listitem";

const Home = ({product}) => {
  
  return <div>
      <Listitem items={product} />
      
  </div>
  
  
};

export default Home;
